/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runWorkflowQueue } from "../funcs/runWorkflowQueue.js";
import { runWorkflowStream } from "../funcs/runWorkflowStream.js";
import { runWorkflowSync } from "../funcs/runWorkflowSync.js";
import { EventStream } from "../lib/event-streams.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Workflow extends ClientSDK {
  /**
   * Workflow - Queue
   *
   * @remarks
   * Create a new workflow run with the given parameters.
   */
  async queue(
    request: components.WorkflowRunRequest,
    options?: RequestOptions,
  ): Promise<components.CreateRunResponse> {
    return unwrapAsync(runWorkflowQueue(
      this,
      request,
      options,
    ));
  }

  /**
   * Workflow - Sync
   *
   * @remarks
   * Create a new workflow run with the given parameters.
   */
  async sync(
    request: components.WorkflowRunRequest,
    options?: RequestOptions,
  ): Promise<Array<components.WorkflowRunOutputModel>> {
    return unwrapAsync(runWorkflowSync(
      this,
      request,
      options,
    ));
  }

  /**
   * Workflow - Stream
   *
   * @remarks
   * Create a new workflow run with the given parameters. This function sets up the run and initiates the execution process. For callback information, see [Callbacks](#tag/callbacks/POST/\{callback_url\}).
   */
  async stream(
    request: components.WorkflowRunRequest,
    options?: RequestOptions,
  ): Promise<EventStream<components.RunStream>> {
    return unwrapAsync(runWorkflowStream(
      this,
      request,
      options,
    ));
  }
}
