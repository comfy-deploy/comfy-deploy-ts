/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runDeploymentStream } from "../../funcs/runDeploymentStream.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.DeploymentRunRequest$inboundSchema,
};

export const tool$runDeploymentStream: ToolDefinition<typeof args> = {
  name: "run-deployment_stream",
  description: `Deployment - Stream

Create a new deployment run with the given parameters. This function sets up the run and initiates the execution process. For callback information, see [Callbacks](#tag/callbacks/POST/{callback_url}).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runDeploymentStream(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
