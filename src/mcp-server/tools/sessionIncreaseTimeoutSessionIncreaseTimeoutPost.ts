/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionIncreaseTimeoutSessionIncreaseTimeoutPost } from "../../funcs/sessionIncreaseTimeoutSessionIncreaseTimeoutPost.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.IncreaseTimeoutBody$inboundSchema,
};

export const tool$sessionIncreaseTimeoutSessionIncreaseTimeoutPost:
  ToolDefinition<typeof args> = {
    name: "session_increase-timeout-session-increase-timeout-post",
    description: `Increase Timeout`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await sessionIncreaseTimeoutSessionIncreaseTimeoutPost(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
