/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileCreateFolderAssetsFolderPost } from "../../funcs/fileCreateFolderAssetsFolderPost.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateFolderRequest$inboundSchema,
};

export const tool$fileCreateFolderAssetsFolderPost: ToolDefinition<
  typeof args
> = {
  name: "file_create-folder-assets-folder-post",
  description: `Create Folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fileCreateFolderAssetsFolderPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
