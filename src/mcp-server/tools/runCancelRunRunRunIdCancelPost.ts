/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runCancelRunRunRunIdCancelPost } from "../../funcs/runCancelRunRunRunIdCancelPost.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CancelRunRunRunIdCancelPostRequest$inboundSchema,
};

export const tool$runCancelRunRunRunIdCancelPost: ToolDefinition<typeof args> =
  {
    name: "run-cancel-run-run-run-id-cancel-post",
    description: `Cancel Run`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await runCancelRunRunRunIdCancelPost(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
