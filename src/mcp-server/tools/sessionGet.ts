/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionGet } from "../../funcs/sessionGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSessionSessionSessionIdGetRequest$inboundSchema,
};

export const tool$sessionGet: ToolDefinition<typeof args> = {
  name: "session_get",
  description: `Get Session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
