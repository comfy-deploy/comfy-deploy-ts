/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionSnapshotSessionSessionSessionIdSnapshotPost } from "../../funcs/sessionSnapshotSessionSessionSessionIdSnapshotPost.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.SnapshotSessionSessionSessionIdSnapshotPostRequest$inboundSchema,
};

export const tool$sessionSnapshotSessionSessionSessionIdSnapshotPost:
  ToolDefinition<typeof args> = {
    name: "session_snapshot-session-session-session-id-snapshot-post",
    description: `Snapshot Session`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await sessionSnapshotSessionSessionSessionIdSnapshotPost(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
