/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sessionCancel } from "../../funcs/sessionCancel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSessionSessionSessionIdDeleteRequest$inboundSchema,
};

export const tool$sessionCancel: ToolDefinition<typeof args> = {
  name: "session-cancel",
  description: `Delete Session`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sessionCancel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
