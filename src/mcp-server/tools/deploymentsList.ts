/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deploymentsList } from "../../funcs/deploymentsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDeploymentsDeploymentsGetRequest$inboundSchema,
};

export const tool$deploymentsList: ToolDefinition<typeof args> = {
  name: "deployments-list",
  description: `Get Deployments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deploymentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
