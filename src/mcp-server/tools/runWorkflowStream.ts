/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runWorkflowStream } from "../../funcs/runWorkflowStream.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.WorkflowRunRequest$inboundSchema,
};

export const tool$runWorkflowStream: ToolDefinition<typeof args> = {
  name: "run-workflow_stream",
  description: `Workflow - Stream

Create a new workflow run with the given parameters. This function sets up the run and initiates the execution process. For callback information, see [Callbacks](#tag/callbacks/POST/{callback_url}).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runWorkflowStream(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
